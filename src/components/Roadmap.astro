---
import { Icon } from 'astro-icon/components'

const steps = [
  {
    title: 'Seamless Workflow Integration',
    description:
      "We are developing an extensive suite of integrations designed to optimize OpenLocalUI's interaction with your operating system. Our objective is to enable users to construct custom data pipelines, facilitating streamlined access and management of their assets with minimal friction.",
    icon: 'mdi:workflow'
  },
  {
    title: 'Advanced Server Configuration',
    description:
      'Future releases will include the capability to configure OpenLocalUI as a robust server environment. This will allow for the hosting and management of language models, providing remote accessibility and centralized control, enhancing the flexibility and scalability of model deployment.',
    icon: 'mdi:server'
  },
  {
    title: 'Developing an Open Ecosystem',
    description:
      'The successful implementation of these features will lay the groundwork for establishing a comprehensive ecosystem centered around our public server API. This ecosystem will promote the development of open-source, privacy-centric applications leveraging self-hosted LLMs, fostering collaboration and innovation within the community.',
    icon: 'mdi:earth'
  }
]
---

<section id='roadmap' class='bg-stone-950 py-20'>
  <div class='container mx-auto px-6'>
    <h2 class='text-3xl font-bold text-white text-center mb-12'>Roadmap</h2>
    <ol class='items-center sm:flex gap-8'>
      {
        steps.map((step) => (
          <li
            class='relative mb-6 sm:mb-0'
            data-aos='fade-up'
            data-aos-delay={steps.indexOf(step) * 150}
          >
            <div class='p-6 rounded-lg transition transform hover:scale-105'>
              <div class='w-16 h-16 mb-6 rounded-full bg-gradient-to-br from-purple-700 to-cyan-700 flex items-center justify-center'>
                <Icon class='w-10 h-10 text-white' name={step.icon} />
              </div>
              <h3 class='text-xl font-semibold mb-2 text-white'>
                {steps.indexOf(step) + 1 + '. ' + step.title}
              </h3>
              <p class='text-gray-300 text-justify'>{step.description}</p>
            </div>
          </li>
        ))
      }
    </ol>
  </div>
</section>
